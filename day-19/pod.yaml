apiVersion: v1
kind: Pod
metadata:
  name: veri-test-pod
spec:
  containers:
    - name: web
      image: nginx
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kalici-disk
  volumes:
    - name: kalici-disk
      persistentVolumeClaim:
        claimName: my-pvc
