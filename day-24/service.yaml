apiVersion: v1
kind: Service
metadata:
  name: my-python-app-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: my-python-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30050
